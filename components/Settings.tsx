import { View, Text } from "react-native";
import React from "react";

const Settings = () => {
  return (
    <View className="pt-12">
      <Text className="text-3xl font-rubik-bold">Einstellungen</Text>
    </View>
  );
};

export default Settings;
